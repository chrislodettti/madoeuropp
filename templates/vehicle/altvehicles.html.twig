{% extends 'base.html.twig' %}
{% block stylesheets %}
  {{ parent() }}

{% endblock %}

{% block body %}
    
     {#  <div class ="filter">
                <div>
                    <form id="buscar-form" action="/" method="GET">
                        <input id="buscar-input" type ="text" name="vehicle" />
                        <input type="submit">
                    </form>
                </div>
                <div>
                    <div>
                        <button>Por Marca</button>
                        <ul class  = "formul">
                            <li>
                                <a>Marcas</a>
                            </li>
                        </ul>
                    </div>
                    <div>
                        <button>Por Modelo</button>
                        <ul class  = "formul">
                            <li>
                                <a>Modelos</a>
                            </li>
                       </ul>
                    </div>
                    <div>
                        <button>Por precio</button>
                        <ul class  = "zoomIn animated ">
                            <li>
                                <a >Precio</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div> #}
    
    <div class="todopack">
    {% for vehicle in vehicles %}
        <div class="thisteamall">
          <div id="inf">
            <div class="infoperfiltitol"><h2>{{ vehicle.modelo }}</h2></div>
            <div class="infotodo">
                <div class="vehiculotodoimg"><p><img class="vehiculologo" src="{{ asset('uploads/pictures/' ~ vehicle.logo) }}"></p></div>
                <h3>Marca: <i>{{ vehicle.marca }}</i></h3>
                <h3>Km: <i>{{ vehicle.Km }}</i></h3>
                <h3>Color: <i>{{ vehicle.color }}</i></h3>
                <h3>Puertas: <i>{{ vehicle.puertas }}</i></h3>
                <h3>Plazas: <i>{{ vehicle.plazas }}</i></h3>
                <h3>Cambio: <i>{{ vehicle.Cambio }}</i></h3>
                <h3>Combustible: <i>{{ vehicle.Combustible}}</i></h3>
                <h3>Price: <i>{{ vehicle.price }}</i></h3>
                  <div class="compraveh">
                   <a href="{{path("detallcompras")}}">Compra Vehiculo</a>
                  </div>
            </div>
        </div>

        {%endfor%}
        
        </div>
        
    </div>
        
{% endblock %}
